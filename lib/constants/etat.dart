// ignore_for_file: camel_case_types

enum EtatEntreprise { open, close }

enum EtatService { open, close }

enum ETAT_ABONNEMENT { main, fournisseur }

enum EtatRequest { loading, loaded, error, none }

enum EtatField { valid, invalid, none }

enum EtatConnexion { open, close, none }
