import 'package:flutter/material.dart';

class PertePage extends StatelessWidget {
  const PertePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('reportes'),
      ),
    );
  }
}
